# Gatsby

# Pages

## Client Only Routes

Without server side rendering, client only pages first route to 404 page. Then
Gatsby runtime fetches page data for the client route and then load the right
component.

# Inner Attributes

```rust
#![crate_type="dylib"]
```

Inner attributes applies to the item that the attribute is declared within (ex.
a crate)

# Outer Attributes

```rust
#[test]
fn test() {

}
```

Outer attributes applies to the item that follows the attribute

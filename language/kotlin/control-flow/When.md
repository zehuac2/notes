# `when`

```kotlin
when (value) {
  boolean expression -> statement
  boolean expression -> {
    statement
    statement
  }
  else -> ...
}
```

- Boolean expressions don't have to be compile time constants

# Installing Packages/Tools

When installing nuget package or tools with `--add-source` option, nuget would
always first search existing sources for the package, and then the sources
passed with the option
